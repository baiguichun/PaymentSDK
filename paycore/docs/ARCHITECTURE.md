# 聚合支付SDK架构设计文档

## 1. 架构概览

聚合支付SDK采用模块化、插件化的架构设计，核心思想是将支付渠道作为独立的SDK模块，实现按需集成。

### 1.1 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                         应用层 (APP)                          │
│  ┌────────────────────────────────────────────────────────┐  │
│  │           Application (初始化、注册渠道)                │  │
│  │           Activity (发起支付、处理结果)                 │  │
│  └────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    核心SDK层 (payment-core)                   │
│  ┌────────────┐  ┌────────────────┐  ┌──────────────────┐   │
│  │ PaymentSDK │  │ChannelManager │  │PaymentSheetDialog│   │
│  │  (入口类)   │  │  (渠道管理)     │  │   (UI组件)       │   │
│  └────────────┘  └────────────────┘  └──────────────────┘   │
│  ┌────────────────────────────────────────────────────────┐  │
│  │          IPaymentChannel (支付渠道接口)                 │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌─────────────────────┐  ┌────────────────────────────┐    │
│  │PaymentLifecycleAct │  │    PaymentApiService      │    │
│  │  (生命周期监听)      │  │    (网络请求)              │    │
│  └─────────────────────┘  └────────────────────────────┘    │
│  ┌────────────────┐  ┌────────────────┐  ┌──────────────┐  │
│  │PaymentLockMgr │  │AppInstall   │                       │
│  │  (并发控制)     │  │  Checker    │                       │
│  └────────────────┘  └──────────────┘                       │
└─────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                  支付渠道SDK层 (独立模块)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ WeChatPay    │  │   Alipay     │  │  UnionPay    │      │
│  │   Channel    │  │   Channel    │  │   Channel    │ ...  │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                   第三方支付SDK层                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  微信开放SDK   │  │  支付宝SDK     │  │   银联SDK     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                         后端服务                              │
│  - 支付渠道配置接口 (根据业务线下发可用渠道)                    │
│  - 支付订单创建接口 (获取预支付参数)                           │
│  - 支付结果通知接口 (接收第三方支付回调)                        │
└─────────────────────────────────────────────────────────────┘
```

## 2. 核心模块设计

### 2.1 PaymentSDK (入口类)

**职责：**
- SDK初始化和配置管理
- 支付渠道注册
- 统一的支付入口
- 全局生命周期管理

**关键方法：**
```kotlin
// 初始化SDK
fun init(app: Application, config: PaymentConfig)

// 注册支付渠道
fun registerChannel(channel: IPaymentChannel)
fun registerChannels(channels: List<IPaymentChannel>)

// 显示支付渠道选择弹窗（自动完成支付）
fun showPaymentSheet(
    activity: Activity,  // ✅ 支持任何Activity
    orderId: String,
    amount: BigDecimal,
    extraParams: Map<String, Any> = emptyMap(),
    businessLine: String? = null,
    onPaymentResult: (PaymentResult) -> Unit,  // ✅ 返回支付结果
    onCancelled: () -> Unit
)

// 使用指定渠道支付（推荐）
fun payWithChannel(
    channelId: String,
    context: Context,
    orderId: String,
    amount: BigDecimal,
    extraParams: Map<String, Any> = emptyMap(),
    onResult: (PaymentResult) -> Unit
)

// suspend版本（已废弃）
@Deprecated("推荐使用回调版本")
suspend fun payWithChannel(...): PaymentResult

// 手动查询订单状态
suspend fun queryOrderStatus(orderId: String): PaymentResult
```

### 2.2 IPaymentChannel (支付渠道接口)

**职责：**
- 定义支付渠道的统一接口
- 封装各支付渠道的差异

**关键属性和方法：**
```kotlin
interface IPaymentChannel {
    val channelId: String          // 渠道唯一标识
    val channelName: String        // 渠道显示名称
    val channelIcon: Int           // 渠道图标
    val requiresApp: Boolean       // 是否需要第三方APP
    val packageName: String?       // APP包名
    val priority: Int              // 优先级
    
    // 执行支付（同步方法）
    fun pay(
        context: Context,
        orderId: String,
        amount: BigDecimal,
        extraParams: Map<String, Any>
    ): PaymentResult
    
    // 检查APP是否安装
    fun isAppInstalled(context: Context): Boolean
}
```

### 2.3 PaymentChannelManager (渠道管理器)

**职责：**
- 管理所有注册的支付渠道
- 渠道查询和过滤
- 验证渠道可用性

**关键方法：**
```kotlin
// 注册渠道
fun registerChannel(channel: IPaymentChannel)

// 获取所有已注册渠道
fun getAllChannels(): List<IPaymentChannel>

// 获取可用渠道（已注册且APP已安装）
fun getAvailableChannels(context: Context): List<IPaymentChannel>

// 根据后端配置过滤可用渠道
fun filterAvailableChannels(
    context: Context,
    channelIds: List<String>
): List<IPaymentChannel>
```

### 2.4 PaymentSheetDialog (支付选择对话框)

> 💡 使用 `BottomSheetDialog` 实现，支持任何类型的Activity

**职责：**
- 展示支付渠道选择UI
- 从后端获取渠道配置
- 过滤并显示可用渠道
- 自动调起支付并返回结果

**工作流程：**
```
1. 显示加载状态
2. 请求后端获取业务线可用渠道配置
3. 过滤出本地已注册的渠道
4. 过滤出APP已安装的渠道
5. 按优先级排序后展示
6. 用户选择渠道
7. 自动调起支付（调用 payWithChannel）
8. 返回支付结果
```

**特性：**
- ✅ 支持任何Activity类型
- ✅ 自动管理协程作用域
- ✅ Dialog关闭时自动取消网络请求
- ✅ 内部集成支付流程

### 2.5 PaymentLifecycleActivity (生命周期监听)

> 💡 透明Activity自动监听支付生命周期

**职责：**
- 监听用户从第三方APP返回
- 自动查询支付结果
- 处理支付结果回调
- 完全透明，对用户无感知

**工作流程：**
```
1. onCreate: 启动透明Activity，发起支付
2. 调起第三方APP（微信/支付宝）
3. onPause: 用户跳转到第三方APP
4. 【用户完成支付】
5. onResume: 检测到用户返回
6. 延迟N秒后查询支付结果
7. 轮询查询后端（最多M次）
8. 返回最终PaymentResult
9. 关闭透明Activity
```

**关键特性：**
- 使用 `ConcurrentHashMap` 管理多个支付回调
- 支持同时进行多个支付
- 自动防止内存泄漏
- 主题设置为完全透明
- Activity 如被系统回收且未能正常结束，会兜底回调 `PaymentResult.Failed("支付流程已中断，请重试")` 并清理回调，避免悬挂

**主题配置：**
```xml
<style name="Theme.PaymentCore.Transparent">
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowIsFloating">true</item>
    <item name="android:backgroundDimEnabled">false</item>
</style>
```

### 2.6 AppInstallChecker (APP安装检测)

**职责：**
- 检测第三方支付APP是否已安装
- 支持批量检测

**关键方法：**
```kotlin
// 检查单个APP
fun isPackageInstalled(context: Context, packageName: String): Boolean

// 批量检查
fun checkMultipleApps(
    context: Context,
    packageNames: List<String>
): Map<String, Boolean>
```

### 2.7 PaymentApiService (网络服务)

**职责：**
- 与后端通信
- 获取支付渠道配置
- 创建支付订单
- 查询订单状态
- 基于 Retrofit + OkHttp 调用，使用 Scalars 转换器获取原始字符串，再用 JSONObject 解析，兼容动态字段（如 extraConfig）
- 保证解析异常直接抛出，调用方可收到 `Result.failure` 处理

**关键方法：**
```kotlin
// 获取支付渠道配置
suspend fun getPaymentChannels(
    businessLine: String,
    appId: String
): Result<List<PaymentChannelMeta>>

// 创建支付订单
suspend fun createPaymentOrder(
    orderId: String,
    channelId: String,
    amount: String,
    extraParams: Map<String, Any>
): Result<Map<String, Any>>

// 查询订单状态（v2.0新增）
suspend fun queryOrderStatus(
    orderId: String,
    paymentId: String? = null
): Result<OrderStatusInfo>
```

### 2.8 PaymentLockManager (并发控制)

> 订单级锁管理，防止重复支付和重复查询

**职责：**
- 管理订单级别的锁
- 防止同一订单重复支付
- 防止同一订单重复查询
- 自动释放锁资源

**关键方法：**
```kotlin
// 尝试锁定订单
fun tryLockOrder(orderId: String): Boolean

// 释放订单锁
fun unlockOrder(orderId: String)

// 检查订单是否被锁定
fun isOrderLocked(orderId: String): Boolean
```

**工作原理：**
```kotlin
// 使用 ConcurrentHashMap 存储正在支付中的订单
private val payingOrders = ConcurrentHashMap.newKeySet<String>()
private val timeoutJobs = ConcurrentHashMap<String, Job>()
private val managerLock = ReentrantLock()
private val timeoutScope = CoroutineScope(SupervisorJob() + Dispatchers.Default)

fun tryLockOrder(orderId: String, timeoutMs: Long = 300000): Boolean {
    managerLock.withLock {
        // 检查订单是否已在支付中
        if (payingOrders.contains(orderId)) {
            return false
        }
        // 标记订单为支付中
        payingOrders.add(orderId)
        
        // 启动超时任务（自动释放锁）
        startTimeoutTask(orderId, timeoutMs)
        
        return true
    }
}

fun unlockOrder(orderId: String) {
    managerLock.withLock {
        payingOrders.remove(orderId)
        // 取消超时任务
        timeoutJobs.remove(orderId)?.cancel()
    }
}

// 超时任务：自动释放锁（防止死锁）
private fun startTimeoutTask(orderId: String, timeoutMs: Long) {
    val timeoutJob = timeoutScope.launch {
        try {
            delay(timeoutMs)  // 等待超时
            managerLock.withLock {
                if (payingOrders.contains(orderId)) {
                    payingOrders.remove(orderId)  // 自动释放
                    timeoutJobs.remove(orderId)
                    onTimeoutCallback?.invoke(orderId)  // 触发回调
                }
            }
        } catch (e: Exception) {
            // 任务被取消，忽略
        }
    }
    timeoutJobs[orderId] = timeoutJob
}
```

**超时自动释放机制：**
- ✅ 防止 APP 崩溃或进程被杀死导致的锁永久持有
- ✅ 订单锁在5分钟后自动释放（可配置）
- ✅ 提升用户体验，用户可以在超时后重新支付

**查询去重机制（v2.0.1新增）：**

为了避免同一订单的并发重复查询，SDK使用 `CompletableDeferred` 实现查询去重：

```kotlin
// PaymentSDK中的查询去重
private val activeQueries = ConcurrentHashMap<String, CompletableDeferred<PaymentResult>>()

private suspend fun queryPaymentResultWithRetry(orderId: String): PaymentResult {
    // 1. 检查是否已有正在进行的查询
    val existingQuery = activeQueries[orderId]
    if (existingQuery != null) {
        // 等待现有查询完成，避免重复请求
        return existingQuery.await()
    }
    
    // 2. 创建新的查询任务
    val queryDeferred = CompletableDeferred<PaymentResult>()
    activeQueries[orderId] = queryDeferred
    
    try {
        // 3. 执行查询
        val result = performActualQuery(orderId)
        
        // 4. 通知所有等待的协程
        queryDeferred.complete(result)
        
        return result
    } finally {
        // 5. 清理查询记录
        activeQueries.remove(orderId)
    }
}
```

**解决的问题：**
- ✅ 自动查询（PaymentLifecycleActivity）+ 手动查询（queryOrderStatus）同时发生
- ✅ 多个协程查询同一订单时，只执行一次实际查询
- ✅ 其他协程等待首次查询结果，避免重复网络请求
- ✅ 自动清理，防止内存泄漏

## 3. 支付流程设计

### 3.1 完整支付流程（v2.0）

```
┌──────────┐
│用户点击支付│
└─────┬────┘
      ▼
┌──────────────────┐
│调用showPaymentSheet│
└─────┬────────────┘
      ▼
┌──────────────────────┐
│显示PaymentSheetDialog │
│     (加载中)          │
└─────┬────────────────┘
      ▼
┌──────────────────────────┐
│请求后端获取可用渠道配置     │
│ GET /api/payment/channels│
│ ?businessLine=xxx&appId=xxx│
└─────┬────────────────────┘
      ▼
┌────────────────────────────┐
│过滤渠道：                    │
│1. 后端返回的channelIds       │
│2. 本地已注册的渠道           │
│3. 自动检测APP已安装的渠道     │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│展示可用支付渠道列表           │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│用户选择支付渠道              │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│SDK自动调用payWithChannel    │
└─────┬──────────────────────┘
      ▼
┌─────────────────────────────────┐
│启动PaymentLifecycleActivity     │
│(透明Activity，用户无感知)         │
└─────┬───────────────────────────┘
      ▼
┌────────────────────────────┐
│检查订单锁(PaymentLockManager)│
│- 已锁定 → 返回失败            │
│- 未锁定 → 加锁并继续          │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│调用 IPaymentChannel.pay()  │
│(普通函数，非suspend)         │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│调起第三方APP                │
│(微信/支付宝/银联)            │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│PaymentLifecycleActivity     │
│onPause - 进入后台            │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│【用户在第三方APP完成支付】     │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│用户点击返回/自动返回          │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│PaymentLifecycleActivity     │
│onResume - 检测到用户返回      │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│延迟查询(initialQueryDelayMs)│
│默认等待3秒                   │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│轮询查询后端支付结果           │
│GET /api/payment/status      │
│?orderId=xxx                 │
│                            │
│最多查询N次，间隔M秒           │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│后端返回订单状态：             │
│- paid → Success            │
│- failed → Failed           │
│- cancelled → Cancelled     │
│- pending/超时 → Processing  │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│释放订单锁                   │
│(PaymentLockManager)         │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│返回PaymentResult            │
│关闭透明Activity              │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│关闭PaymentSheetDialog       │
└─────┬──────────────────────┘
      ▼
┌────────────────────────────┐
│onPaymentResult回调           │
│用户处理最终结果               │
└────────────────────────────┘
      ▼
┌──────────────────────┐
│展示可用渠道列表        │
└─────┬────────────────┘
      ▼
┌──────────────────┐
│用户选择支付渠道    │
└─────┬────────────┘
      ▼
┌──────────────────────────┐
│调用channel.pay()执行支付   │
└─────┬────────────────────┘
      ▼
┌──────────────────────────┐
│调起第三方支付APP           │
│(微信/支付宝/银联等)         │
└─────┬────────────────────┘
      ▼
┌──────────────────────────┐
│用户在第三方APP完成支付      │
└─────┬────────────────────┘
      ▼
┌──────────────────────────┐
│返回支付结果               │
│- Success: 支付成功         │
│- Failed: 支付失败          │
│- Cancelled: 用户取消       │
└─────┬────────────────────┘
      ▼
┌──────────────────────────┐
│APP处理支付结果             │
│查询后端确认支付状态        │
└──────────────────────────┘
```

### 3.2 渠道过滤逻辑

```kotlin
fun filterChannels(
    backendChannelIds: List<String>,    // 后端配置
    registeredChannels: List<IPaymentChannel>, // 本地注册
    context: Context                     // 用于检测APP安装
): List<IPaymentChannel> {
    return backendChannelIds
        .mapNotNull { channelId ->
            // 1. 必须是本地已注册的渠道
            registeredChannels.find { it.channelId == channelId }
        }
        .filter { channel ->
            // 2. 如果需要APP，则必须已安装
            !channel.requiresApp || channel.isAppInstalled(context)
        }
        .sortedByDescending { it.priority } // 3. 按优先级排序
}
```

## 4. 模块化设计

### 4.1 核心SDK模块 (payment-core)

**必须集成**，包含：
- SDK入口和配置
- 支付渠道接口定义
- 渠道管理器
- UI组件
- 网络服务
- 工具类

### 4.2 支付渠道SDK模块

**按需集成**，每个渠道一个独立模块：

- `payment-channel-wechat`: 微信支付
- `payment-channel-alipay`: 支付宝
- `payment-channel-union`: 银联支付
- `payment-channel-custom`: 自定义渠道

**优势：**
1. 应用可以只集成需要的支付渠道
2. 减小APK体积
3. 避免集成不必要的第三方SDK
4. 易于扩展新渠道

### 4.3 依赖关系

```gradle
// APP依赖
dependencies {
    implementation project(':payment-core')          // 核心SDK（必须）
    implementation project(':payment-channel-wechat') // 按需
    implementation project(':payment-channel-alipay') // 按需
    implementation project(':payment-channel-union')  // 按需
}

// 渠道SDK依赖
dependencies {
    api project(':payment-core')           // 依赖核心SDK
    implementation 'com.tencent.mm:wechat-sdk' // 第三方SDK
}
```

## 5. 扩展性设计

### 5.1 新增支付渠道

只需3步即可集成新的支付渠道：

**步骤1：创建渠道SDK模块**
```
payment-channel-custom/
├── build.gradle
└── src/main/java/com/payment/channel/custom/
    └── CustomPayChannel.kt
```

**步骤2：实现IPaymentChannel接口**
```kotlin
class CustomPayChannel : IPaymentChannel {
    override val channelId = "custom_pay"
    override val channelName = "自定义支付"
    override val channelIcon = R.drawable.ic_custom
    override val requiresApp = true
    override val packageName = "com.custom.pay"
    
    override fun pay(
        context: Context,
        orderId: String,
        amount: BigDecimal,
        extraParams: Map<String, Any>
    ): PaymentResult {
        // 调起第三方支付APP
        val intent = Intent().apply {
            // 设置支付参数
        }
        context.startActivity(intent)
        
        // 立即返回Success，实际结果由SDK通过后端查询获取
        return PaymentResult.Success(orderId)
    }
}
```

**步骤3：在Application中注册**
```kotlin
PaymentSDK.registerChannel(CustomPayChannel())
```

### 5.2 自定义UI

可以不使用默认的半屏弹窗，自己实现选择界面：

```kotlin
// 1. 获取可用渠道
val channels = PaymentSDK.getAvailableChannels(context)

// 2. 展示自定义UI
showCustomChannelPicker(channels) { selectedChannel ->
    // 3. 执行支付
    selectedChannel.pay(...)
}
```

## 6. 安全性设计

### 6.1 数据安全

1. **敏感信息不存储**：支付参数仅在内存中传递，不持久化
2. **HTTPS通信**：所有网络请求使用HTTPS
3. **签名验证**：支付参数包含签名，防止篡改

### 6.2 代码混淆

```proguard
# 核心类不混淆
-keep class com.payment.core.PaymentSDK { *; }
-keep interface com.payment.core.channel.IPaymentChannel { *; }
-keep class com.payment.core.PaymentResult { *; }

# 渠道实现类不混淆（便于反射查找）
-keep class * implements com.payment.core.channel.IPaymentChannel { *; }
```

## 7. 性能优化

### 7.1 异步处理

- 所有网络请求使用Kotlin Coroutines异步执行
- UI操作在主线程，支付逻辑在IO线程

### 7.2 懒加载

- 支付渠道SDK按需加载
- UI组件按需创建

### 7.3 内存优化

- 使用单例模式管理核心组件
- 及时释放不需要的资源

## 8. 测试策略

### 8.1 单元测试

- 渠道管理器测试
- APP安装检测测试
- 网络服务模拟测试

### 8.2 集成测试

- 完整支付流程测试
- 多渠道切换测试
- 异常场景测试

### 8.3 UI测试

- 半屏弹窗显示测试
- 渠道列表渲染测试
- 用户交互测试

## 9. 后端接口设计

### 9.1 获取支付渠道配置

```
GET /api/payment/channels

请求参数:
- businessLine: 业务线标识
- appId: 应用ID

响应:
{
  "code": 0,
  "data": [
    {
      "channelId": "wechat_pay",
      "channelName": "微信支付",
      "enabled": true,
      "iconUrl": "https://...",
      "extraConfig": {}
    }
  ]
}
```

### 9.2 创建支付订单

```
POST /api/payment/create

请求参数:
{
  "orderId": "订单ID",
  "channelId": "支付渠道ID",
  "amount": "金额",
  "extraParams": {}
}

响应:
{
  "code": 0,
  "data": {
    // 微信支付参数
    "prepay_id": "...",
    "sign": "...",
    // 或支付宝参数
    "order_info": "...",
    // 或银联参数
    "tn": "..."
  }
}
```

## 10. 总结

聚合支付SDK通过模块化、插件化的架构设计，实现了：

✅ **灵活集成**：应用可按需选择支付渠道  
✅ **易于扩展**：新增渠道只需实现接口  
✅ **业务隔离**：不同业务线使用不同渠道配置  
✅ **自动验证**：自动检测APP安装状态  
✅ **友好UI**：提供开箱即用的选择界面  
✅ **高可维护**：模块独立，职责清晰  
